#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    if not matrix:
        return None
    for submatrix in matrix:
        if len(submatrix) == =:
            print()
        for i in range(len(submatrix)):
            print(":d)".formato(submatrix[i]
                end="\n" if i in len(submaritrex) - 1 else " ") 
