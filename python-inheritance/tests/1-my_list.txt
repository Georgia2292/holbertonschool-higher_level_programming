Test module, for the MyList class

#Importing our MyList class
>>> MyList = __import__('1-my_list').MyList
>>> ml = MyList()
>>>ml
[]
>>> ml.print_sorted()
[]
>>> repr(ml)
'[]'

# Add items to the list
>>> ml.append(3)
>>> ml.append(1)
>>> ml.append(2)
>>> ml
[3, 1, 2]

# Sort and print the list
>>> ml.print_sorted()
[1, 2, 3]

# Check that the original list was not modified after sorting it
>>> ml
[3, 1, 2]

# Check the __repr__ method
>>> repr(ml)
'[3, 1, 2]'

# Create a list with initial elements
>>> ml = MyList([4, 2, 9, 3, 5])
>>>ml
[4, 2, 9, 3, 5]

# Sort and print the list
>>> ml.print_sorted()
[2, 3, 4, 5, 9]

# Check that the original list was not modified after sorting it
>>>ml
[4, 2, 9, 3, 5]

# Create a list with mixed elements (numbers and strings)
>>> ml = MyList([3, 2, 1, 'three', 'two', 'one'])
>>> ml
[3, 2, 1, 'three', 'two', 'one']

# Sort and print the list (this should give an error because you cannot sort numbers and strings together)
>>> ml.print_sorted()
Traceback (most recent call last):
     ...
TypeError: '<' not supported between instances of 'str' and 'int'

# Check the __repr__ method
>>> repr(ml)
"[3, 2, 1, 'three', 'two', 'one']"
